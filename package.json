{
  "name": "@dolfdijkstra/oce-management-api-cli-generator",
  "version": "3.0.0",
  "description": "Generator for the management api cli tools.",
  "main": "src/cli.js",
  "scripts": {
    "format": "prettier-standard 'src/*.js' || true",
    "check": "standard --fix 'src/*.js' || true",
    "fnc": "prettier-standard 'src/*.js' && standard --fix 'src/*.js' || true"
  },
  "author": "Dolf Dijkstra",
  "license": "MIT",
  "bin": {
    "oce-management-api-client-generator": "./src/cli.js"
  },
  "dependencies": {
    "commander": "^4.1.0",
    "copy-dir": "^1.2.0",
    "lodash": "^4.17.21",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "prettier-standard": "^16.2.1",
    "standard": "^14.3.3"
  }
}
